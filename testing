git clone <address> # to create a local copy of the repo.
# One member of the team should add any files to the folder and then 
# commit those changes. 
git add . git 
commit -m “initial files”

git push  # use it with the default settings OR 
git push <source-name> <name-of-branch> # specify the source-name
# which is typically “origin” and name-of-branch # which is typically “master”

